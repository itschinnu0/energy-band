# Phase 13 — Final Windows Packaging

## Objective
Produce the final `Energy-Band-Simulator.exe`.

## Tasks
- finalize launcher
- configure PyInstaller
- include required Streamlit/Matplotlib assets
- resolve hidden imports
- build executable
- test outside development environment
- test on a clean Windows environment where Python is not installed
- verify browser auto-launch
- verify all three devices
- verify exports
- document build procedure

## Acceptance
The EXE launches reliably and the simulator functions without requiring a Python installation.
